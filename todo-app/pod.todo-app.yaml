apiVersion: v1
kind: Pod
metadata:
  name: todo-app
  labels:
    app: todo-app
spec:
  containers:
    - name: todo-app
      image: sberston/todo-app:latest
      #    imagePullPolicy: Always
      ports:
        - name: http
          containerPort: 8080